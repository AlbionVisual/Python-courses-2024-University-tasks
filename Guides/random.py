import random

print(random.random()) # Случайное число от 0 до 1
print(random.randrange(4)) # Случайное целое число от 0 до 4
print(random.uniform(1,100)) # Случайное вещественное число от 1 до 100
print(random.choice(['1el', '2el', '3el'])) # Случайный элемент списка
